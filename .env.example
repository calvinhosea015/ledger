SUPABASE_URL=https://fcuailkqitbaruqachsp.supabase.co
SUPABASE_ANON_KEY=sb_publishable_…

# Optional table name overrides (defaults shown):
# SUPABASE_CATEGORIES_TABLE=categories
# SUPABASE_ITEMS_TABLE=items
# SUPABASE_PROFILES_TABLE=profiles
# SUPABASE_ENVELOPES_TABLE=envelopes

# Local demo without Supabase (data resets when the app quits):
# USE_FAKE_BACKEND=true

# Never put the Supabase service_role key in the Flutter client or commit it.
# Values are passed via --dart-define (or Env defaults), not loaded from this file.
